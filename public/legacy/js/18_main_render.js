/* =========================
   Main render
   ========================= */
function renderAll() {
  renderWallets();
  renderDailyBudget();
  renderTransactions();
  renderSettings();
  redrawCharts();
}

